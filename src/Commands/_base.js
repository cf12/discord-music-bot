exports.handler = (client, msg, args, guild, env, modules) => {
  const ms = modules.messageSender
  const pf = env.prefix
}

exports.info = {
  command: '',
  fullCommand: '',
  shortDescription: '',
  longDescription: ''
}
